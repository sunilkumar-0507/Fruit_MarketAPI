# Caddy automatically obtains & renews a Let's Encrypt certificate for this domain.
# {$API_DOMAIN} is read from the environment (set in .env / docker-compose.prod.yml).
{$API_DOMAIN} {
	reverse_proxy api:8080
	encode gzip
}
